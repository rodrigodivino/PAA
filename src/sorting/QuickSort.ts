export default class QuickSort implements SortingAlgorithm {
    public array: number[]
    constructor(array: number[]){
        this.array = array
    }
    sort(): number[] {
        return []
    }
}